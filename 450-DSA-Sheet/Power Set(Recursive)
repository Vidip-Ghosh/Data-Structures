//https://practice.geeksforgeeks.org/problems/power-set4302/1
#include<bits/stdc++.h>
using namespace std;

class Solution{
    vector<string> ans;
    void recur(int idx,string v,string &s)
    {
        if(v.size())
        {
            ans.push_back(v);
        }
        for(int i=idx;i<s.size();i++)
        {
            v+=s[i];
            recur(i+1,v,s);
            v.pop_back();
        }
    }
	public:
		vector<string> AllPossibleStrings(string s){
		  recur(0,"",s);
		  sort(ans.begin(),ans.end());
		  return ans; 
		}
};

int main(){
	int tc;
	cin >> tc;
	while(tc--){
		string s;
		cin >> s;
		Solution ob;
		vector<string> res = ob.AllPossibleStrings(s);
		for(auto i : res)
			cout << i <<" ";
		cout << "\n";

	}
	return 0;
}
//TC --> O(2^n)
//SC --> O(2^n)
