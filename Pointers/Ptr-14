#include <iostream>

using namespace std;

int main()
{
    int arr[] = {11,21,13,14};
    int *ptr = arr;
    
    cout<<sizeof(arr)<<" "<<sizeof(ptr)<<endl;
    cout<<*(arr)<<" "<<*(arr+1)<<endl;
    cout<<arr<<" "<<&arr<<" "<<(arr+1)<<endl;
    cout<<ptr[0]<<" "<<ptr[1]<<" "<<ptr[2]<<endl;
}
